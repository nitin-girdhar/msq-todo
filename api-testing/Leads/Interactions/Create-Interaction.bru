meta {
  name: Create Interaction
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/leads/{{leadId}}/interactions
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "interaction_type": "call",
    "notes": "Discussed product details with lead"
  }
}
