meta {
  name: Cancel Request
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/hr/leave/requests/{{leaveRequestId}}/cancel
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "comment": "Plans changed"
  }
}
