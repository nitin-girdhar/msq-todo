meta {
  name: Reject Request
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/hr/leave/requests/{{leaveRequestId}}/reject
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "comment": "Insufficient coverage that week"
  }
}
