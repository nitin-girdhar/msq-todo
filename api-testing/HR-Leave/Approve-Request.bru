meta {
  name: Approve Request
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/hr/leave/requests/{{leaveRequestId}}/approve
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "comment": "Approved"
  }
}
