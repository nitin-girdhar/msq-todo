meta {
  name: Update Leave Settings
  type: http
  seq: 22
}

put {
  url: {{baseUrl}}/hr/leave/settings
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "leave_cycle_start_month": 4,
    "scope": "org"
  }
}
