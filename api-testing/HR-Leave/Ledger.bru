meta {
  name: Ledger
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/hr/leave/ledger?userId={{targetUserId}}&page=1&limit=50
  body: none
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}
