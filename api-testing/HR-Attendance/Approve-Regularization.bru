meta {
  name: Approve Regularization
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/hr/attendance/regularizations/{{regularizationId}}/approve
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "comment": "Confirmed present"
  }
}
