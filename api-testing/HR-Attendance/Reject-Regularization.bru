meta {
  name: Reject Regularization
  type: http
  seq: 19
}

post {
  url: {{baseUrl}}/hr/attendance/regularizations/{{regularizationId}}/reject
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "comment": "No supporting evidence"
  }
}
