meta {
  name: List Orgs (Filtered)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/orgs
  body: none
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

params:query {
  ~cityIds:
  ~stateIds:
  ~countryIds:
}
