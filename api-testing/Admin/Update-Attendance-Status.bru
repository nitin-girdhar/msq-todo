meta {
  name: Update Attendance-Status
  type: http
  seq: 54
}

patch {
  url: {{baseUrl}}/lookups/attendance-statuses/{{attendanceStatusId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "On Leave (Updated)",
    "is_active": true
  }
}
