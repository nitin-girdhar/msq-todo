meta {
  name: Update Org-Types
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/lookups/org-types/{{orgTypeId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "Franchise (Updated)",
    "is_active": true
  }
}
