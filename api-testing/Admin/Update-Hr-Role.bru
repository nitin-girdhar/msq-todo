meta {
  name: Update Hr-Role
  type: http
  seq: 60
}

patch {
  url: {{baseUrl}}/lookups/hr-roles/{{hrRoleId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "HR Recruiter (Updated)",
    "is_active": true
  }
}
