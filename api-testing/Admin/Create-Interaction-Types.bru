meta {
  name: Create Interaction-Types
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/lookups/interaction-types
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "name": "sms",
    "label": "SMS",
    "description": "Text message interaction"
  }
}
