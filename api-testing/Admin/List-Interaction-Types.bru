meta {
  name: List Interaction-Types
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/lookups/interaction-types?tenant_id={{tenantId}}
  body: none
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}
