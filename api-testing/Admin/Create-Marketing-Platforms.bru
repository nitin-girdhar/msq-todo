meta {
  name: Create Marketing-Platforms
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/lookups/marketing-platforms
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "name": "google_ads",
    "label": "Google Ads",
    "description": "Google Ads platform"
  }
}
