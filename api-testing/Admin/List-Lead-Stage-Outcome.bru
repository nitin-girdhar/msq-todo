meta {
  name: List Lead-Stage-Outcome
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/lookups/lead-stage-outcome?tenant_id={{tenantId}}
  body: none
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}
