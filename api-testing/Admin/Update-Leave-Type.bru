meta {
  name: Update Leave-Type
  type: http
  seq: 48
}

patch {
  url: {{baseUrl}}/lookups/leave-types/{{leaveTypeId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "Sabbatical (Updated)",
    "is_active": true
  }
}
