meta {
  name: Update Lead-Sources
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/lookups/lead-sources/{{leadSourceId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "Referral (Updated)",
    "is_active": true
  }
}
