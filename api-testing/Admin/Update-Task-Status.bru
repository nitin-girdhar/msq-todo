meta {
  name: Update Task-Status
  type: http
  seq: 45
}

patch {
  url: {{baseUrl}}/lookups/task-statuses/{{taskStatusId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "On Hold (Updated)",
    "is_active": true
  }
}
