meta {
  name: List Task-Priorities
  type: http
  seq: 42
}

get {
  url: {{baseUrl}}/lookups/task-priorities?tenant_id={{tenantId}}
  body: none
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}
