meta {
  name: Update Campaign-Statuses
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/lookups/campaign-statuses/{{campaignStatusId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "Paused (Updated)",
    "is_active": true
  }
}
