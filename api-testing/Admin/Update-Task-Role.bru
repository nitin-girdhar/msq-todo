meta {
  name: Update Task-Role
  type: http
  seq: 63
}

patch {
  url: {{baseUrl}}/lookups/task-roles/{{taskRoleId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "Task Observer (Updated)",
    "is_active": true
  }
}
