meta {
  name: Create Employment-Type
  type: http
  seq: 50
}

post {
  url: {{baseUrl}}/lookups/employment-types?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "name": "intern",
    "label": "Intern",
    "description": "Fixed-term intern"
  }
}
