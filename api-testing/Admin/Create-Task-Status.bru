meta {
  name: Create Task-Status
  type: http
  seq: 41
}

post {
  url: {{baseUrl}}/lookups/task-statuses?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "name": "on_hold",
    "label": "On Hold",
    "is_terminal": false,
    "sort_order": 6
  }
}
