meta {
  name: Update Lead-Stage
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/lookups/lead-stage/{{leadStageId}}?tenant_id={{tenantId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "label": "On Hold (Updated)",
    "sort_order": 8,
    "is_active": true
  }
}
