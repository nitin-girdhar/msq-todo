meta {
  name: Update Organizations
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/lookups/organizations/{{organizationId}}
  body: json
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

body:json {
  {
    "name": "Downtown Branch (Updated)",
    "is_active": true
  }
}
